# Lint configuration for the Open-H-Endoluminal contribution-guide scripts.
# Run with:  ruff check scripts/
line-length = 120

[lint]
# Ruff defaults (E4/E7/E9/F) plus import sorting (I) and whitespace hygiene (W).
extend-select = ["I", "W"]
